// Leftover scratch file from an automated verification pass (compiled every .svelte file
// with the Svelte compiler directly, bypassing a broken sandbox-only rollup/rolldown native
// binding). Safe to delete — not used by the app. `rm .tmp-svelte-check.mjs`
